name: fl_components
description: Components de flutter

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.1 <4.0.0"

dependencies:
  animate_do: ^3.0.2
  cupertino_icons: ^1.0.5
  flutter:
    sdk: flutter
  font_awesome_flutter: ^10.4.0

dev_dependencies:
  flutter_lints: ^2.0.1
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/
